schema_version: 1
id: BL-002
family: Baseline
title: ADD 后 P3/P4/P5 BottleneckRefine 基线
parent_id: BL-001
lifecycle:
  status: auto
  recovery_confidence: confirmed
hypothesis: 在逐尺度 ADD 融合后增加轻量残差 Bottleneck，可在进入 FPN/PAN 前改善同尺度局部特征表达。
change_from_parent: 在 P3、P4、P5 的 ADD 输出后分别增加一个 e=0.5、卷积核 1x1/3x3 的 Bottleneck 精炼块。
architecture:
  summary: RGB/IR -> P3/P4/P5 ADD -> BottleneckRefine
  version: v1
  stages:
  - P3
  - P4
  - P5
  heads: HNA
  flow:
    P3: RGB C2f + IR C2f_Faster -> ADD -> Bottleneck(e=0.5, k=1/3)
    P4: RGB C2f + IR C2f_Faster -> ADD -> Bottleneck(e=0.5, k=1/3)
    P5: RGB SPPF + IR SPPF -> ADD -> Bottleneck(e=0.5, k=1/3)
files:
  model_yaml: yaml/yolov8s-baseline-ADD-P345-BottleneckRefine.yaml
  train_entrypoint: train_dronevehicle_baseline_add_bottleneck_refine.py
  migration_script: tools/make_twostream_obb_weights.py
  init_checkpoint: pre-pth/yolov8s-obb_twostream_baseline_add_p345_bottleneck_refine.pt
  module_files:
  - ultralytics/nn/modules/block.py
  integration_files:
  - ultralytics/nn/modules/__init__.py
  - ultralytics/nn/tasks.py
training:
  profile: experiments/profiles/dronevehicle_obb_100e.yaml
  launch_mode: checkpoint
  trainer_class: ultralytics.models.yolo.obb.train:OBBTrainer
  output:
    project: runs/DroneVehicle_OBB/train-labels=official-v1/baseline/mainline/BL-002__bottleneck-refine-p345
    name: attempt=01
    layout: seeded-v1
legacy:
  run_dir: null
  relocated_runs: experiments/run_registry.yaml
reports: []
notes:
- 正确加载迁移 checkpoint 的重跑目录以相同实验名自动递增为 v12。
- PT-R006 重跑记录与本 Manifest 表示同一个永久实验，不另分配实验 ID。
